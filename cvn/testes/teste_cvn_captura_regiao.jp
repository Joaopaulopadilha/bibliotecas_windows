importar cvn
x = 1920 / 2
y = 1080 / 2
x1 = x - 150
x2 = x + 150
y1 = y - 150
y2 = y + 150

enquanto verdadeiro:
    regiao = cvn.capturar_regiao(x1, y1, 300, 300)
    cvn.exibir("captura", regiao)
    
    tecla = cvn.esperar(1)  # Espera 1ms só para processar eventos
    
    # Libera a imagem anterior para não vazar memória
    cvn.liberar(regiao)
    

cvn.fechar_todas()