importar gdx

janela = gdx.janela("Aplicação", 600, 600)
#imagem de fundo opcional, pode ser jpg, png, jpeg...
#(id, caminho da imagem de fundo)
gdx.janela_imagem(janela, "tumb.png")
botao = gdx.botao("mudar imagem", 100, 30, 250, 250)

loop:
    se gdx.botao_clicado(botao):
        gdx.janela_imagem_atualizar(janela, "thum.png")
    
    
    # Atualiza visual do botão (Importante no Linux)
    gdx.botao_exibir(botao)
    
    gdx.janela_exibir(janela)