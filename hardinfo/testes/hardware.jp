importar hardinfo
importar os

os.limpar_terminal()

saida_verde("===Cpu===")
cpu = hardinfo.cpu()
saida(cpu)

saida_verde("===Gpu===")
gpu = hardinfo.gpu()
saida(gpu)

saida_verde("===,Memória===")
memoria = hardinfo.memoria()
saida(memoria)

saida_verde("===Armazenamento===")
hd = hardinfo.hd()
saida(hd)

