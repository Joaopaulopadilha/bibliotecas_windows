# 3_sqlite_ler.jp
# Passo 3: Ler os dados gravados anteriormente

importar sqlite

# 1. Abre o mesmo banco
db = sqlite.abrir("primeiro_banco_de_dados.db")

saida("--- Conteúdo da Tabela ---")

# 2. Faz a consulta de tudo o que tem na tabela 'registros'
# A função consultar retorna uma string única com o cabeçalho e os dados
resultado = sqlite.consultar(db, "SELECT * FROM registros")

# 3. Mostra o resultado na tela
saida(resultado)

# 4. Fecha o banco
sqlite.fechar(db)