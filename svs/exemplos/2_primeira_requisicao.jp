importar svs

porta = svs.criar(8080)
ip = svs.meu_ip()

saida("servindo em: {ip}:{porta}")

enquanto verdadeiro:
    req = svs.aguardar(porta)
    saida("=== Requisição recebida ===")
    saida("Método: " + svs.metodo(req))
    saida("Caminho: " + svs.caminho(req))
    svs.fechar(req)